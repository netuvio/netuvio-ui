services:
  storybook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: netuvio-ui-storybook
    restart: unless-stopped
    ports:
      - "7481:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
